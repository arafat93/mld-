// config.example.js — template for config.js
//
// 1. Copy this file and rename the copy to "config.js"
// 2. Fill in your own Firebase project's values below
//    (find them in Firebase Console → Project Settings → General → Your apps → SDK setup and configuration)
// 3. config.js is gitignored, so your real keys never get pushed to GitHub
//
// DASHBOARD_GITHUB.html will not work until config.js exists with valid values.

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.firebasestorage.app",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
